# 后端 v2 实施计划与裁定

用户批准日期：2026-09-18；基线 cc3e48ee300ba56db6bf79af43e07d2f297b667f。
旧服务镜像 sha256:d4ef39c208b61fc89e6885cba8cbf35437e06b839cc9aefc63f6cd76b49f7c93，端口 3000，不修改该部署。

## 固定决策

- 新服务 /api/v2、独立 compose 项目、127.0.0.1:3001、data-v2；旧 API 不在新服务挂载。不开发新 UI。
- 邀请注册长期账号；账号库和审计库分离。异步 scrypt N=2^17 r=8 p=1，随机盐，密码计算并发最多 2。随机会话、HttpOnly/SameSite Cookie、7 天有效期，生产 HTTPS/Secure。
- 维护者命令管理一次性邀请码（7 天）、密码重置与撤销。邀请码和凭证只存校验值，不进入日志。
- 账号登录不自动接管；显式接管才能恢复席位，旧设备的 HTTP/Socket/语音权限撤销。局中退出保留席位和计时，房主退出不解散。
- 普通观战只看公开信息；第二屏由玩家授权，兑换码 5 分钟一次性使用，每玩家最多一名私人观众。参与同局的账号不能观看其他玩家私有视角。
- 规则 2.0：首日先按入夜名单竞选，再公布死讯和晨间效果；公告前不得因权限暴露死亡。
- 晨间/放逐处理完复活、转阶段等效果即判胜；成立立即结束，取消后续遗言及移交。非终局夜死天理在普通发言前移交；首夜/白天遗言在移交之前。
- 普通/竞选发言 120 秒，准备最多 15 秒，可主动开始；技能及回归 30 秒，阵营 60 秒，指定发言顺序单独 45 秒，其余不变。固定夜间窗口不提前结束。
- 团队截止优先最新全员确认方案；否则最后合法草稿（含空刀）；没有草稿空刀。
- 账号持久保存，但不承诺服务器重启恢复对局。空闲无人连接大厅 2 小时、终局 24 小时回收，日志独立保留。

## 小迭代顺序

01 工程基线；02 权限查询；03 合法目标；04 公开投影；05 通道一致性；06 窗口实例；07 截止与队列；08 幂等；09 天理移交；10 立即终局；11 首日竞选换序；12 计时；13 团队兜底；14 账号存储；15 认证；16 席位接管退出；17 公开观战；18 私人第二屏；19 配置/限流/回收/指标各自提交；20 定向集成、容量及候选镜像。

每小步独立短分支、测试通过后提交并快进合入 backend/v2；不推送、不合并远端、不发布。规则 1.1 保留用于旧回归，v2 规则显式选择，不能热切换对局快照。

## 验证协议

由 gpt-5.6-luna 子代理负责约定范围内的测试；主代理定义预期、实现、审阅证据。子代理只改测试文件，不弱化预期、不自行全量运行或换模型。测试冻结被测源码，记录 SHA、差异哈希、命令、结果和未覆盖项。
复用锁定依赖镜像，挂载工作树源码，内存或临时 SQLite，使用假时钟。普通小步仅选择测试文件；发布构建保留一次全量单测门禁，不执行旧 UI 全量 E2E。
容量验收：2 CPU/4 GB 限制，100 账号、两桌 26 玩家+24 观众，外置测试客户端持续 5 分钟。普通 API P95<300ms、事件循环 P99<100ms、RSS<1GB、无异常 5xx/重复操作/泄密；登录和语音负载另记。

进度与每步证据见 backend-v2-progress.md。工作区 Issue 汇总仅作为入口。
