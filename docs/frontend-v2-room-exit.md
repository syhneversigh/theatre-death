# 房间退出对齐（2026-09-19）

本次将 GitHub main `596a171` / `c36e8d1` 的终局退出体验与新版账号、原房间连续对局结合。用户确定：保留局中暂离；终局最后正式玩家退出后立即关闭房间，剩余观众同时退出。注册简化另行实施。

## 各阶段行为

| 阶段 | 退出入口与行为 | 房主及房间生命周期 |
| --- | --- | --- |
| 大厅 | 离开房间，释放名额，保持账号登录 | 房主可独立选择解散；普通离开按在线正式成员规则继任，无正式成员后5分钟清理 |
| 局中 | 房间管理 → 暂离对局；保持本局玩家身份和席位，回来恢复本人，计时和行动窗口不暂停 | 不通过暂离终止对局；无正式成员后仍有5分钟恢复期；离线本身不算退出 |
| 终局复盘 | 复盘页直接离开房间，也保留房间管理入口；返回首页而不退出账号 | 有其他正式成员时保留复盘、可继任房主；最后正式成员退出后立即解散并通知剩余观众 |

终局退出者仍可在房间存在且本局复盘未结束时用原账号回来查看；这保留新版恢复能力，不采用旧版“退出后永远无法回看”的提示。最后一名正式成员退出后，旧房间码失效，不能重入。

房主“结束复盘，返回大厅”与“离开房间”分别展示：前者将当前成员一起带回原大厅，保留房间码和规则，重新准备和分配身份；后者只退出本人，除最后正式成员外不会关闭他人的复盘。没有在线继任者时暂时无房主，之后按现有规则恢复。

## 实现边界与接口

- `/leave` 继续使用 `requestId`；响应保留 `{left:true, seatRetained:boolean}`，房间已关闭时不返回保留席位的误导信息。
- HTTP 成功或实时终端通知后才离开页面；响应丢失保留原意图，重试使用同一请求ID及载荷，不调用登出或结束复盘接口。
- 退出确认文案按大厅、进行中、复盘以及观战身份区分，复盘不再显示“对局继续计时”。
- 终局无正式成员清理取消旧空房定时器，保留已完成对局审计记录，撤销剩余观众的访问；不清理账户数据库。
- 房间销毁后不再重新注册该局的媒体授权，避免已经撤销的语音房间状态残留。
- 没有合并或改写旧版退出实现；新版通过自己的成员、授权、继任及幂等机制满足对齐后的行为。

## 验证

由指定 Luna 测试代理在 Docker 中完成以下增量验证：

- 房间生命周期、退出幂等、房主继任及前端房间模型：6文件38项通过（`empty-rooms`、`room-membership`、`room-rounds`、`room-operation-api`、`room-governance`、`frontend-v2-room-model`）。
- 维护扫描及 OpenAPI：2文件9项通过（`v2-maintenance`、`contract-openapi`）。
- 根 TypeScript 与新版前端 TypeScript 检查通过。
- `07-review.spec.ts`：Chromium 7项、WebKit 7项全部通过，0失败、0跳过；包括取消退出不发请求、失败保留复盘、同一requestId重试，以及原有复盘/管理/下一局入口回归。浏览器中的复盘数据采用隔离夹具；服务端立即关闭、观众访问收回、账号仍登录和退出回执重放另由HTTP测试验证。
- 截图：`test-results-frontend-v2/review-exit-confirmation-chromium.png`、`test-results-frontend-v2/review-exit-confirmation-webkit.png`。

测试使用独立 `td-exit-alignment` Docker 项目和合成数据，测试结束已移除该项目容器与网络，保留测试卷。未构建/部署新镜像，未跑全量回归，未切换本地5174，也未操作公网服务。
